'use client';

import DashboardLayout from './DashboardLayout/index';

// Forward the component and re-export it from the same file
// This ensures backward compatibility with existing imports
export default DashboardLayout;
